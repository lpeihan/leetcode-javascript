import { mySqrt, mySqrt_0 } from '../src/069E. Sqrt(x)';

test('mySqrt', () => {
  expect(mySqrt(4)).toBe(2);
  expect(mySqrt(8)).toBe(2);
});

test('mySqrt_0', () => {
  expect(mySqrt_0(4)).toBe(2);
  expect(mySqrt_0(8)).toBe(2);
});
